version: 2

sphinx:
  fail_on_warning: true

python:
  version: 3.7
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - docs/requirements.txt